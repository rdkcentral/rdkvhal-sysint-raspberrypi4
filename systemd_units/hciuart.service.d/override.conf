[Unit]
Requires=dev-ttyAMA0.device
After=dev-ttyAMA0.device

[Service]
ExecStartPre=/bin/sh -c 'for i in $(seq 1 5); do [ -e /dev/ttyAMA0 ] && exit 0; sleep 1; done; exit 1'
Restart=on-failure
RestartSec=2
StartLimitInterval=0